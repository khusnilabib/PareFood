# PareFood Platform shared analysis options (PF-DOC-23 §3.1).
# Every package includes this file via its own analysis_options.yaml.
include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    avoid_print: error
    invalid_annotation_target: warning
  exclude:
    - "**/generated/**"
    - "**/*.g.dart"
    - "**/*.freezed.dart"

linter:
  rules:
    # Style / maintainability (CS-R01, CS-R05)
    always_declare_return_types: true
    avoid_print: true
    avoid_void_async: true
    directives_ordering: true
    prefer_final_locals: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_single_quotes: true
    use_key_in_widget_constructors: true
    sort_child_properties_last: true
    unnecessary_lambdas: true
    unnecessary_string_interpolations: true
    # Async correctness (FL-R01, FL-R02)
    unawaited_futures: true
    cancel_subscriptions: true
    # Correctness
    prefer_relative_imports: true
